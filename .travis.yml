language: php
dist: precise
php:
  - 5.6
  - 5.5
  - 5.4
  - 5.3
  - hhvm

matrix:
  allow_failures:
    - php: hhvm

before_script:
  - composer install

script:
  - if [[ ${TRAVIS_PHP_VERSION:0:3} >= "5.5" ]]; then
  -   ./vendor/bin/phplint
  - fi
  - phpunit --colors test
